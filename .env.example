# Local Workers runtime uses .dev.vars; hosted runtime uses secret environment settings.
# Generate 32 random bytes encoded as 64 hexadecimal characters; do not rotate after saving connections without migrating them.
CREDENTIAL_ENCRYPTION_KEY=
# MCP/monitor process only. Never include these in client bundles.
POSITION_AGENT_URL=http://localhost:3000
POSITION_AGENT_TOKEN=
